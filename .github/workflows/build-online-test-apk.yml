name: Build Online Test APK

on:
  workflow_dispatch:
  push:
    branches:
      - agent/online-apk-ready
  pull_request:
    paths:
      - "autoload/**"
      - "gameplay/**"
      - "network/**"
      - "scenes/**"
      - "tests/**"
      - "tools/configure_android_ci.gd"
      - "ui/**"
      - "config/backend_config.json"
      - "project.godot"
      - "export_presets.cfg"
      - ".github/workflows/build-online-test-apk.yml"

concurrency:
  group: online-test-apk-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-online-apk:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    permissions:
      contents: read
    env:
      GODOT_VERSION: "4.6.3"
      GODOT_BIN: ${{ github.workspace }}/.godot-bin/godot
      ANDROID_SDK_ROOT: /usr/local/lib/android/sdk
      ANDROID_HOME: /usr/local/lib/android/sdk
      GODOT_ANDROID_KEYSTORE_DEBUG_PATH: ${{ github.workspace }}/colony-debug.keystore
      GODOT_ANDROID_KEYSTORE_DEBUG_USER: androiddebugkey
      GODOT_ANDROID_KEYSTORE_DEBUG_PASSWORD: android

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: Install Godot and Android templates
        run: |
          set -euo pipefail
          chmod +x tools/install_godot_ci.sh
          tools/install_godot_ci.sh
          "$GODOT_BIN" --version
          TEMPLATE_DIR="$HOME/.local/share/godot/export_templates/${GODOT_VERSION}.stable"
          test -f "$TEMPLATE_DIR/android_debug.apk"

      - name: Configure Android SDK and debug signing
        run: |
          set -euo pipefail
          SDKMANAGER="$ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager"
          test -x "$SDKMANAGER"
          required_android_paths=(
            "$ANDROID_SDK_ROOT/platform-tools/adb"
            "$ANDROID_SDK_ROOT/build-tools/35.0.0/aapt2"
            "$ANDROID_SDK_ROOT/platforms/android-35/android.jar"
          )
          missing_android_packages=0
          for required_path in "${required_android_paths[@]}"; do
            if [ ! -e "$required_path" ]; then
              missing_android_packages=1
              break
            fi
          done
          if [ "$missing_android_packages" -eq 1 ]; then
            timeout 120 bash -o pipefail -c \
              'yes | "$1" --licenses >/dev/null' _ "$SDKMANAGER" || true
            timeout 600 "$SDKMANAGER" --sdk_root="$ANDROID_SDK_ROOT" \
              "platform-tools" \
              "build-tools;35.0.0" \
              "platforms;android-35"
          fi
          for required_path in "${required_android_paths[@]}"; do
            test -e "$required_path"
          done
          keytool -keyalg RSA -genkeypair \
            -alias androiddebugkey \
            -keypass android \
            -keystore "$GODOT_ANDROID_KEYSTORE_DEBUG_PATH" \
            -storepass android \
            -dname "CN=Android Debug,O=Colony Dominion,C=TR" \
            -validity 10000 \
            -deststoretype PKCS12
          "$GODOT_BIN" --headless --editor --path . \
            --script res://tools/configure_android_ci.gd

      - name: Import and compile Godot project
        run: |
          set -euo pipefail
          mkdir -p build/online-test
          "$GODOT_BIN" --headless --path . --editor --quit 2>&1 | tee build/online-test/import.log
          if grep -Eq 'SCRIPT ERROR:|Parse Error:|Compile Error:|Failed to load script' build/online-test/import.log; then
            exit 1
          fi

      - name: Run online regression tests
        run: |
          set -euo pipefail
          tests=(
            tests/query_safe_url_regression_test.gd
            tests/online_foundation_regression_test.gd
            tests/online_security_contract_test.gd
            tests/online_transport_regression_test.gd
            tests/online_presentation_parity_regression_test.gd
            tests/online_production_completion_test.gd
            tests/google_oauth_bot_backfill_test.gd
          )
          for test_path in "${tests[@]}"; do
            "$GODOT_BIN" --headless --path . --script "res://$test_path"
          done

      - name: Verify live Edgegap matchmaking boundary
        run: |
          set -euo pipefail
          python - <<'PY'
          import json
          import urllib.error
          import urllib.parse
          import urllib.request
          from pathlib import Path

          config = json.loads(Path("config/backend_config.json").read_text(encoding="utf-8"))
          base = str(config["supabase_url"]).rstrip("/")
          publishable_key = str(config["supabase_publishable_key"]).strip()

          def endpoint(path: str) -> str:
              return f"{base}/functions/v1/matchmaking/{path.lstrip('/')}"

          def get_json(path: str) -> dict:
              request = urllib.request.Request(
                  endpoint(path),
                  headers={
                      "Accept": "application/json",
                      "Cache-Control": "no-cache",
                      "apikey": publishable_key,
                  },
              )
              with urllib.request.urlopen(request, timeout=30) as response:
                  if response.status != 200:
                      raise SystemExit(f"{path} returned HTTP {response.status}")
                  return json.loads(response.read().decode("utf-8"))

          health = get_json("/health")
          if health.get("ok") is not True or health.get("service") != "colony-edgegap-matchmaking":
              raise SystemExit(f"Edgegap matchmaking health failed: {health}")

          payload = json.dumps({
              "player_id": "00000000-0000-4000-8000-000000000001",
              "display_name": "APKSmoke",
              "region_preference": "auto",
              "selected_region_id": "auto",
          }).encode("utf-8")
          unauthorized = urllib.request.Request(
              endpoint("/join"),
              data=payload,
              method="POST",
              headers={
                  "Accept": "application/json",
                  "Content-Type": "application/json",
                  "apikey": publishable_key,
              },
          )
          try:
              urllib.request.urlopen(unauthorized, timeout=30)
          except urllib.error.HTTPError as error:
              if error.code != 401:
                  raise SystemExit(f"Auth boundary returned HTTP {error.code}") from error
          else:
              raise SystemExit("Unauthenticated matchmaking request was accepted")

          report = {
              "health": True,
              "edgegap_configured": bool(health.get("configured")),
              "placement_targets": [
                  region.get("id")
                  for region in config.get("regions", [])
                  if region.get("enabled")
              ],
              "auth_boundary": True,
              "build_id": config["build_id"],
              "protocol_version": config["protocol_version"],
          }
          Path("build/online-test/live-control-smoke.json").write_text(
              json.dumps(report, indent=2, ensure_ascii=False) + "\n", encoding="utf-8"
          )
          print(json.dumps(report))
          PY

      - name: Build Android online test APK
        run: |
          set -uo pipefail
          mkdir -p build/android build/online-test
          set +e
          "$GODOT_BIN" --headless --path . \
            --export-debug "Android" \
            build/android/colony-dominion-online-test.apk \
            2>&1 | tee build/online-test/android-export.log
          EXPORT_CODE=${PIPESTATUS[0]}
          set -e
          echo "$EXPORT_CODE" > build/online-test/android-export-exit-code.txt
          if [ "$EXPORT_CODE" -ne 0 ]; then
            tail -n 120 build/online-test/android-export.log >&2 || true
            exit "$EXPORT_CODE"
          fi
          test -s build/android/colony-dominion-online-test.apk
          unzip -t build/android/colony-dominion-online-test.apk >/dev/null
          sha256sum build/android/colony-dominion-online-test.apk \
            | tee build/android/SHA256SUMS.txt
          stat --printf='%s\n' build/android/colony-dominion-online-test.apk \
            > build/android/apk-size-bytes.txt

      - name: Upload online APK and diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: colony-dominion-online-test-apk
          if-no-files-found: warn
          retention-days: 14
          path: |
            build/android/colony-dominion-online-test.apk
            build/android/SHA256SUMS.txt
            build/android/apk-size-bytes.txt
            build/online-test/live-control-smoke.json
            build/online-test/import.log
            build/online-test/android-export.log
            build/online-test/android-export-exit-code.txt
